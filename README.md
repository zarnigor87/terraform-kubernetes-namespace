# Usage
```
module "example" {
  source          = "zarnigor87/namespaces/kubernetes"
  name            = "my-first-namespace"
  number_of_pods  = 36
}
```

# Commands
```
terraform init
terraform apply
```